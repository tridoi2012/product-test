import React from "react";
import "./header.css";

const Horizontal = (props) => {
  return (
    <div className="menu-button">
      <button>Bonus UI</button>
      <button>Level Menu</button>
    </div>
  );
};

export default Horizontal;
